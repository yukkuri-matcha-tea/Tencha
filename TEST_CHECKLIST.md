# Tencha 1.5.8 実機チェック

## 0. 事前準備

- LINEが26.13.0であることを確認する。
- 重要データをバックアップする。
- VectorでTenchaを有効化し、スコープをLINEだけにする。
- 最初は「おすすめ設定」のみで確認する。
- 変更のたびにLINEを完全終了して再起動する。

## 1. 接続と復旧

1. LINEを起動してからモジュールAPKを開く。
2. 「Vector接続」が「接続済み」になることを確認する。
3. 「診断・復旧」でHook失敗やSafe Modeがないことを確認する。
4. 「次回起動のみ全拡張OFF」をONにし、次回LINE起動で拡張が適用されないことを確認する。
5. さらにLINEを再起動し、通常設定へ復帰することを確認する。

## 1.1 Tencha本体UI

- ホーム上部のTop App Bar、接続状態、LINE・モジュールのバージョンが欠けずに表示される。
- 下部ナビゲーションの「ホーム」「機能」「診断」「情報」で正しい画面へ移動できる。
- 画面幅320 / 360 / 390dpと文字サイズを大きくした状態で、文字や操作ボタンが重ならない。
- Android 12以降では壁紙由来のDynamic Colorが反映され、ライト／ダークモードでも標準Color Roleのコントラストが保たれる。
- Themed IconをONにすると、LINE内設定と同じ葉っぱの単色アイコンへシステムの色が適用される。
- Themed IconをOFFにすると、背景と前景が分離したカラーAdaptive Iconになる。
- 「Tenchaについて」からGitHub、X、YouTubeが正しいURLで開く。
- 非公式モジュールである旨とバックアップに関する注意書きが読める。

## 2. おすすめ設定

- トーク時刻が`HH:mm:ss`になる。
- 通常のWeb URLが既定ブラウザで開く。
- ログインやアカウント連携URLはLINE内のまま動作する。
- トークリスト広告、ホームおすすめ、サービス欄が消える。
- AgentI関連アイコンが消える。
- トーク内で1文字検索できる。

## 3. 既読

必ずテスト用アカウントとのトークで行う。

- 既読回避ONで受信トークを開き、相手側に既読が付かない。
- 「送信後既読」をONにして返信すると既読が送信される。
- 手動既読操作では既読が送信される。
- 既読回避OFFでは純正動作へ戻る。
- 「次の1トークだけ既読回避」を予約し、最初に開いたトークだけ5分間既読が付かない。
- 別のトークには一時既読回避が波及しない。
- 本アイコン長押しで「このトークの既読回避: ON」が表示され、そのトークだけ再起動後も既読が付かない。
- Tenchaの「既読回避中のトークを管理」から個別解除できる。
- グループ既読履歴をONにし、既読ユーザー名と時刻が記録される。

## 4. 送信取消

- 相手が取得済みテキストを取り消しても、自端末に取消表示付きで残る。
- 取消前に取得していない内容は復元されない。
- 画像・動画・ファイルは端末が取得済みの場合だけ確認する。
- 自分の取消時間延長は、サーバーが拒否した場合に成功扱いしない。

## 5. UI・検索・メディア

- 非表示にしたタブだけが消え、ホームとトークへ移動できる。
- タブラベル非表示後もタップ範囲が正常である。
- メンバー指定検索と1文字検索を別々に確認する。
- 高品質画像は送信前後の解像度とファイルサイズを比較する。
- 5分超動画はアップロード開始とサーバー受理を分けて記録する。

## 6. 通知

FCM Fixは最後に単独で確認する。

- 画面ON/OFF、Doze、Wi-Fi、モバイル回線で通知時刻を記録する。
- 重複通知がない。
- バッテリー消費と常駐通知を確認する。
- 問題があればFCM FixだけをOFFにする。

## 7. 保存とバックアップ

- 保存先を選んでいない状態でも、LINE起動時にファイル・フォルダ選択画面が出ない。
- LINEを再起動してもTenchaの設定が維持される。
- 旧版で保存先を指定済みの場合、既存データがTencha内部ストレージへ一度だけ移行される。
- 「機能設定」からバックアップを押すと、端末フォルダまたはGoogle Driveを選べるシステム保存画面が開く。
- ローカルへ保存した`.tencha.zip`を「復元」から選び、設定と履歴が戻る。
- Google Driveアプリへログイン済みの場合、保存先にGoogle Driveを選んで同じ確認を行う。
- 復元後はLINEを完全終了して再起動する。
- バックアップには履歴が含まれるため、作成したファイルを第三者へ共有しない。
- LINE内の「トーク履歴のバックアップ」を実行すると、保存先未設定でも成功する。
- 上記操作ではファイル選択画面を開かず、最新スナップショットをTencha内部領域へ保存する。
- 2回目のバックアップに失敗した場合、直前に成功した内部スナップショットが失われない。
- Tencha本体から書き出した`.tencha.zip`に、内部トーク履歴バックアップが存在する場合だけ同梱される。
- LINE内の「トーク履歴の復元」はTencha内部の最新スナップショットを使い、未作成時は明確に通知する。

## 8. 開発者モード

- 機能設定の最下部に「開発者向け」「開発者モード」が表示される。
- OFFではプロフィール内部情報、FCM実験、LINE常駐、バージョン／署名互換機能が表示されない。
- ONにすると開発者向け項目がその場で展開される。
- OFFに戻すと保存済みの開発者向け機能も次回LINE起動では適用されない。
- 通常利用後は開発者モードをOFFへ戻す。

## 問題が起きたとき

1. モジュールAPKで「次回起動のみ全拡張OFF」を予約する。
2. LINEを完全終了して再起動する。
3. 起動できたら「診断・復旧」で失敗したFeatureを確認する。
4. 問題FeatureだけをOFFにする。
5. それでも起動できない場合はVector側で本モジュールを無効化する。

報告時は、LINEバージョン、端末、Android版、ONにした機能、再現手順、診断画面の状態を添付する。メッセージ本文や相手の個人情報は含めない。

## 9. 非root / LSPatch

- 「Tenchaについて」から「rootなしで使う」を開ける。
- LSPatch Manager 1.1以上なら「インストール済み」、0.6など旧版なら「更新が必要」と表示される。
- Shizuku未導入でも「任意」と表示され、Tenchaの通常画面がエラーにならない。
- LSPatchとShizukuのボタンが正しい配布元またはインストール済みアプリを開く。
- 非root手順に、署名違いで公式LINEへ直接上書きできないことが明記される。
- Tenchaから公式LINEのアンインストールやアプリデータ削除が実行されない。
- パッチしたbase APKと全split APKが同一証明書で署名される。
- 物理端末検証では、別のテスト環境を用意し、起動・ログイン・Tencha接続・各Hookを個別に確認する。
